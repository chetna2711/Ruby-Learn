<div class="container mt-5">
  <h1 class="mb-4">New Student</h1>

  <div class="card">
    <div class="card-body">
      <%= render "form", student: @student %>
    </div>
  </div>

  <div class="mt-3">
    <%= link_to 'Back to Students List', students_path, class: "btn btn-secondary" %>
  </div>
</div>
